<nav class="navbar navbar-expand-lg navbar-light" id="mainNav">
            <div class="container px-4 px-lg-5">
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
                    Menu
                    <i class="fas fa-bars"></i>
                </button>
                <div class="collapse navbar-collapse" id="navbarResponsive">
                    <!-- DEBUG (hidden): isLoggedIn = <%= typeof isLoggedIn !== 'undefined' ? isLoggedIn : 'undefined' %>, isAdmin = <%= typeof isAdmin !== 'undefined' ? isAdmin : 'undefined' %> -->
                    <ul class="navbar-nav ms-auto py-4 py-lg-0">
                        <li class="nav-item"><a class="nav-link px-lg-3 py-3 py-lg-4" href="/">Exercices</a></li>
                        <li class="nav-item"><a class="nav-link px-lg-3 py-3 py-lg-4" href="/games/mastermind">🎮 Jeu</a></li>
                        <li class="nav-item"><a class="nav-link px-lg-3 py-3 py-lg-4" href="/about">About</a></li>
                        <li class="nav-item"><a class="nav-link px-lg-3 py-3 py-lg-4" href="/contact">Contact</a></li>
                        <li class="nav-item"><a class="nav-link px-lg-3 py-3 py-lg-4" href="/search">Search</a></li>
                        
                        <!-- Admin-only links -->
                        <% if (typeof isAdmin !== 'undefined' && isAdmin) { %>
                            <li class="nav-item"><a class="nav-link px-lg-3 py-3 py-lg-4" href="/posts/new"><i class="fas fa-plus"></i> Create</a></li>
                            <li class="nav-item"><a class="nav-link px-lg-3 py-3 py-lg-4" href="/admin/dashboard">Dashboard</a></li>
                        
                        <% } %>
                        
                        <!-- Authentication links -->
                        <% if (typeof isLoggedIn !== 'undefined' && isLoggedIn) { %>
                            <li class="nav-item">
                                <a class="nav-link px-lg-3 py-3 py-lg-4" href="#" id="logoutLink">
                                    <i class="fas fa-sign-out-alt"></i> Logout
                                </a>
                                <form id="logoutForm" action="/users/logout" method="post" style="display: none;">
                                </form>
                            </li>
                        <% } else { %>
                            <li class="nav-item"><a class="nav-link px-lg-3 py-3 py-lg-4" href="/users/login"><i class="fas fa-sign-in-alt"></i> Login</a></li>
                            <li class="nav-item"><a class="nav-link px-lg-3 py-3 py-lg-4" href="/users/register"><i class="fas fa-user-plus"></i> Register</a></li>
                        <% } %>
                    </ul>
                </div>
            </div>
</nav>

<script>
document.addEventListener('DOMContentLoaded', function() {
    const logoutLink = document.getElementById('logoutLink');
    if (logoutLink) {
        logoutLink.addEventListener('click', function(event) {
            event.preventDefault();
            handleLogout();
        });
    }
});

function handleLogout() {
    try {
        const form = document.getElementById('logoutForm');
        if (form) {
            form.submit();
        } else {
            console.error('Logout form not found');
            // Fallback: redirect to logout URL
            window.location.href = '/users/logout';
        }
    } catch (error) {
        console.error('Logout error:', error);
        // Fallback: redirect to logout URL
        window.location.href = '/users/logout';
    }
}
</script>