# Production Environment Variables for Heroku
# Copy these to your Heroku Config Vars

# Server Configuration
PORT=                               # Heroku sets this automatically
NODE_ENV=production

# Database Configuration
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/clean-blog-database?retryWrites=true&w=majority

# Session Configuration (Generate strong secrets for production)
SESSION_SECRET=your-super-strong-session-secret-here-minimum-32-characters

# Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-blog-email@gmail.com
SMTP_PASS=your-gmail-app-password
CONTACT_EMAIL=your-contact-email@gmail.com

# Security Configuration
CORS_ORIGIN=https://your-app-name.herokuapp.com
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Heroku Deployment Instructions:
# 1. Install Heroku CLI: https://devcenter.heroku.com/articles/heroku-cli
# 2. Login: heroku login
# 3. Create app: heroku create your-app-name
# 4. Set config vars: heroku config:set KEY=VALUE
# 5. Deploy: git push heroku main